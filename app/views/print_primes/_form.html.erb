<%= form_for(@print_prime) do |f| %>
  <% if @print_prime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@print_prime.errors.count, "error") %> prohibited this print_prime from being saved:</h2>

      <ul>
      <% @print_prime.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
